{
  "name": "@scamper/server",
  "version": "4.1.1",
  "description": "The Scamper file server: per-user storage behind the FS interface",
  "private": true,
  "type": "module",
  "license": "ISC",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "db:migrate": "better-auth migrate --config src/auth-config.ts",
    "account": "tsx src/admin.ts"
  },
  "dependencies": {
    "better-auth": "^1.6.26",
    "kysely": "^0.28.17",
    "mysql2": "^3.23.2",
    "tsx": "^4.23.1"
  },
  "devDependencies": {
    "@better-auth/cli": "^1.4.21"
  }
}
